version: '3.8'

services:
  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    container_name: todo-keycloak
    environment:
      KEYCLOAK_ADMIN: ${USER}
      KEYCLOAK_ADMIN_PASSWORD: ${PASSWORD}
    command: start-dev
    ports:
      - "8080:8080"


